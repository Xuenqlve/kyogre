
1、解析命令行参数：获取用户输入的配置文件路径、场景文件路径、压测时间等参数。
2、加载配置文件：读取YAML或JSON格式的配置文件，包括数据库连接信息和压测场景配置。
3、验证配置：检查配置的合法性，如数据库连接参数、压测线程数、操作比例等。
4、初始化数据库连接池：根据数据库类型（MySQL、MongoDB、Redis）创建连接池。
5、初始化数据生成器：根据数据模式（schema）配置，初始化相应的数据生成器（随机生成、顺序生成等）。
6、初始化场景：根据场景配置，初始化各阶段的工作负载。
7、启动指标收集器：启动一个独立的goroutine用于收集和聚合指标。
8、启动工作线程：根据配置的线程数，启动多个工作线程，每个线程按照操作比例执行数据库操作。
9、压测计时器：启动一个计时器，当压测时间到达设定值时，发出停止信号。
10、执行数据库操作：工作线程执行插入、更新、删除、查询等操作。
11、记录指标：每次操作后，记录操作类型、耗时、成功与否等指标。
12、停止工作线程：计时器时间到，通知所有工作线程停止。
13、等待线程结束：等待所有工作线程安全退出。
14、生成报告：根据收集的指标数据，生成压测报告。







